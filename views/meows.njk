{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
    <div class="card">
        <h1>Varukorg</h1>

        <main class="wrapper">
            <h1>{{ title }}</h1>
            <p>Jag kan nu byta namn på min route till /cats för att visa en lista på alla katter.</p>
            <p>Nästa steg blir sedan att göra sidor för varje enskild katt.</p>
            
              <ul class="reviewlist">
                {% for dish in dishes %}
                <li><a href="/dishes/{{dish.id}}">{{ dish.name }}</a><a href="/dishes/{{ dish.id }}/delete">Remove review</a>
                </li>
                {% endfor %}

                <h2>{{part.name}}</h2>
    {% if part.image %}
    <img src="/img/{{produkt.image}}" alt="{{part.name}}">
    {% endif %}

            </ul>
          </main>

        {% for meow in meows %}
        <ul>
            <li>
                <h2> {{ meow.username }} </h2>
                <p> {{ meow.content }} </p>
            </li>
        </ul>

        {% endfor %}
    </div>
</main>
{% endblock %}